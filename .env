# Для локальной разработки на Apple Silicon используйте: linux/arm64
# Для production серверов (x86_64) используйте: linux/amd64
PLATFORM=linux/amd64

# Фронт можно собирать отдельно (часто проще держать amd64 из-за зависимостей).
# Локально на Apple Silicon обычно ок: linux/amd64 (через эмуляцию) или linux/arm64.
FRONTEND_PLATFORM=linux/amd64

# Database
POSTGRES_DB=laundry_db
POSTGRES_USER=laundry_user
POSTGRES_PASSWORD=laundry_password
